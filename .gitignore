# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment variables
.env

# Supabase CLI local cache
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
!.env.example

# Android TWA project (generated app wrapper)
android/node_modules
android/app/build
android/build
android/.gradle
android/local.properties
android/*.keystore
android/gen-key.mjs
android/*.apk
android/*.apk.idsig
android/*.aab

# Chrome extension dev signing key (only needed locally to keep the
# unpacked extension ID stable across reloads -- see extension/README.md)
extension/*.pem

# Claude Code local settings (can contain locally-approved permission rules
# that echo sensitive values, e.g. an exported token) -- never share these.
.claude/settings.local.json
