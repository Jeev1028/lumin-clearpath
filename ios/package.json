{
  "name": "clearpath-ios",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor iOS wrapper for ClearPath -- loads the live site (see capacitor.config.json) rather than bundling built web assets, mirroring the Android TWA's 'render the live site' approach.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0",
    "@capacitor/keyboard": "^8.0.5"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
